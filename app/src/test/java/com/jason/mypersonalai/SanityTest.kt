package com.jason.mypersonalai

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder unit test for milestone 1. Its only job is to prove
 * ./gradlew test is correctly wired to the JVM test source set.
 * Real logic (agent orchestration, tool registry, etc.) will get
 * real tests in the milestones that introduce them.
 */
class SanityTest {
    @Test
    fun `project is wired up`() {
        assertEquals(4, 2 + 2)
    }
}
