// Top-level build file. No project-wide dependencies declared here;
// versions are pinned via the plugins block so app/build.gradle.kts
// stays simple and there is a single source of truth per plugin.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
